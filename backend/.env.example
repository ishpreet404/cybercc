# Cyber Chaukidaar - Central Backend Environment Variables

PORT=8787
HOST=0.0.0.0

# Optional: Threat Intelligence / OSINT Breach Checker API Key
# If provided, /api/breach-check will forward queries to the live LeakOSINT upstream service.
# If omitted or left empty, the server automatically uses the built-in offline breach dataset fallback.
LEAKOSINT_API_KEY=
